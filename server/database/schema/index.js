const StoreModel = require('./Store.schema');
const ProductModel = require('./Product.schema');
const BrandModel = require('./Brand.schema');

module.exports = {
  StoreModel,
  ProductModel,
  BrandModel
};